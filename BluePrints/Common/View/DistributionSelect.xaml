<UserControl x:Class="BluePrints.Views.DistributionSelect" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core" xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors" xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid" xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol" xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" x:Name="userControl" Width="1000" Height="300" MaxHeight="900">
    <dxlc:LayoutControl Orientation="Vertical">
        <dxlc:LayoutGroup Name="LayoutGroupMain" Header="Edit Value" Orientation="Vertical" View="Group">
            <dxlc:LayoutGroup Name="LayoutGroupContent" Height="50" Orientation="Vertical" View="Group">
                <dxlc:LayoutItem Height="50" Label="Select Curve:">
                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                        <dxe:ListBoxRadioButton Name="Equal" Padding="5" Content="Equal" IsChecked="True">
                            <dxmvvm:Interaction.Behaviors>
                                <dxmvvm:EventToCommand Command="{Binding DataContext.RadioCheckedCommand, ElementName=userControl}" EventName="Checked" PassEventArgsToCommand="True" />
                            </dxmvvm:Interaction.Behaviors>
                        </dxe:ListBoxRadioButton>
                        <dxe:ListBoxRadioButton Name="Front" Padding="5" Content="Front Loaded">
                            <dxmvvm:Interaction.Behaviors>
                                <dxmvvm:EventToCommand Command="{Binding DataContext.RadioCheckedCommand, ElementName=userControl}" EventName="Checked" PassEventArgsToCommand="True" />
                            </dxmvvm:Interaction.Behaviors>
                        </dxe:ListBoxRadioButton>
                        <dxe:ListBoxRadioButton Name="Balanced" Padding="5" Content="Balanced">
                            <dxmvvm:Interaction.Behaviors>
                                <dxmvvm:EventToCommand Command="{Binding DataContext.RadioCheckedCommand, ElementName=userControl}" EventName="Checked" PassEventArgsToCommand="True" />
                            </dxmvvm:Interaction.Behaviors>
                        </dxe:ListBoxRadioButton>
                        <dxe:ListBoxRadioButton Name="Back" Padding="5" Content="Back Loaded">
                            <dxmvvm:Interaction.Behaviors>
                                <dxmvvm:EventToCommand Command="{Binding DataContext.RadioCheckedCommand, ElementName=userControl}" EventName="Checked" PassEventArgsToCommand="True" />
                            </dxmvvm:Interaction.Behaviors>
                        </dxe:ListBoxRadioButton>
                        <Label Padding="20,5,10,0" Content="Curve Begin %" />
                        <dxe:SpinEdit EditValue="{Binding DataContext.CurveBeginPercentage, ElementName=userControl}" Increment="0.1" Mask="p2" MaskUseAsDisplayFormat="True" MaxValue="0.9" MinValue="0" ToolTip="Specify which % to begin using curve profile, only applicable when curve is used">
                            <dxmvvm:Interaction.Behaviors>
                                <dxmvvm:EventToCommand Command="{Binding SpinEditValueChangingCommand}" EventName="EditValueChanging" PassEventArgsToCommand="True" />
                            </dxmvvm:Interaction.Behaviors>
                        </dxe:SpinEdit>
                    </StackPanel>
                </dxlc:LayoutItem>
            </dxlc:LayoutGroup>
            <dxlc:LayoutGroup>
                <dxlc:LayoutItem />
            </dxlc:LayoutGroup>
            <dxlc:LayoutGroup>
                <dxlc:LayoutItem>
                    <dxg:GridControl x:Name="gridControl" MaxHeight="500" AutoGenerateColumns="AddNew" ItemsSource="{Binding SimulationTable}">
                        <dxmvvm:Interaction.Behaviors>
                            <dxmvvm:EventToCommand Command="{Binding DataContext.AutoGeneratingPercentageColumnsCommand, ElementName=userControl}" EventName="AutoGeneratingColumn" PassEventArgsToCommand="True" />
                        </dxmvvm:Interaction.Behaviors>
                        <dxg:GridControl.Columns>
                            <dxg:GridColumn FieldName="Entity.SubJob.Code" Fixed="Left" Header="Subjob" ReadOnly="True" VisibleIndex="10" />
                            <dxg:GridColumn FieldName="Entity.Discipline.Code" Fixed="Left" Header="Discipline" ReadOnly="True" VisibleIndex="20" />
                        </dxg:GridControl.Columns>
                        <dxg:GridControl.View>
                            <dxg:TableView x:Name="tableView" AllowEditing="False" AllowGrouping="False" NewItemRowPosition="None" ShowTotalSummary="True" />
                        </dxg:GridControl.View>
                    </dxg:GridControl>
                </dxlc:LayoutItem>
            </dxlc:LayoutGroup>
            <dxlc:LayoutGroup Name="LayoutGroupButtons" Height="30" HorizontalAlignment="Right" Orientation="Horizontal" View="Group" />
        </dxlc:LayoutGroup>
    </dxlc:LayoutControl>
</UserControl>